# Bitnami PostgreSQL for Temporal persistence (local Kind)
architecture: standalone

auth:
  username: temporal
  password: temporal
  database: temporal

primary:
  persistence:
    enabled: true
    size: 2Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
